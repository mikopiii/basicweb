## Packages
framer-motion | Smooth animations for hero and scroll reveals

## Notes
Tailwind config needs to extend fontFamily to support 'Outfit' and 'DM Sans'
Static avatar placeholder needs to be handled gracefully if missing
